<!-- Generated by agents-reverse-engineer v1.2.17 -->

# src

Core graph database engine implementing Cypher-inspired query language with PEG parser, discriminated union AST, type-safe schema system, and step-based execution pipeline. Exports query parsing via `parseQueryToSteps`, AST-to-step conversion via `astToSteps`, runtime graph operations via `Graph` class, and modular traversal steps via `steps/` subdirectory.

## Query Language & Parsing

**[grammar.peggy](./grammar.peggy)** — PEG grammar defining Cypher-inspired syntax: node patterns `(n:Label)`, edge patterns `-[r:REL]->`, variable-length paths `*min..max`, WHERE conditions, RETURN clauses, and 50+ keywords (case-insensitive via `"WORD"i !IdentifierPart`).

**[grammar.js](./grammar.js)** — Peggy 5.1.0-generated parser exporting `parse(input, options)`, `SyntaxError` class with `format(sources)` method, and `StartRules: ["MultiStatement"]`. Returns AST nodes discriminated by `type` field.

**[grammar.d.ts](./grammar.d.ts)** — Type declarations for `Location`, `LocationRange`, `SyntaxError`, `ParserTracer`, and `parse()` overloads returning `Query | UnionQuery | MultiStatement`.

**[AST.ts](./AST.ts)** — Discriminated union AST node types: `Query`, `UnionQuery`, `MultiStatement`, `MatchClause`, `Pattern`, `NodePattern`, `EdgePattern`, `ShortestPathPattern`, `CreateClause`, `MergeClause`, `SetClause`, `DeleteClause`, `RemoveClause`, `ForeachClause`, `CallClause`, `WithClause`, `UnwindClause`, `ReturnClause`, `GroupByClause`, `OrderByClause`, and 40+ expression types (`PropertyCondition`, `ArithmeticExpression`, `FunctionCall`, `ListComprehension`, `PatternComprehension`, `QuantifierExpression`, `ReduceExpression`, `MapProjection`, `ExistsSubquery`).

## Schema & Type System

**[GraphSchema.ts](./GraphSchema.ts)** — Type-safe schema definitions with Standard Schema v1 property validation. Exports `GraphSchema`, `VertexSchema`, `EdgeSchema`, `PropertySchema<TInput, TOutput>`, `IndexConfig` (hash/btree/fulltext), and inference utilities (`VertexLabel`, `EdgeLabel`, `VertexProperties`, `EdgeProperties`, `PropertiesFromSchema`).

**[TemporalTypes.ts](./TemporalTypes.ts)** — Cypher temporal value classes: `DateValue`, `LocalTimeValue`, `TimeValue`, `LocalDateTimeValue`, `DateTimeValue`, `DurationValue`. ISO 8601 parsing via regex patterns `/^(\d{4})-(\d{2})-(\d{2})$/` for dates, `/^(\d{2}):(\d{2}):(\d{2})(?:\.(\d{1,9}))?(Z|([+-])(\d{2}):(\d{2}))$/` for time with offsets, and ISO 8601 duration format `P[n]Y[n]M[n]DT[n]H[n]M[n]S`. Exports arithmetic operations (`addDuration`, `subtractDuration`, `durationBetween`, `durationInMonths`, `durationInDays`, `durationInSeconds`) and `TruncateUnit` type.

## Core Runtime

**[Graph.ts](./Graph.ts)** — Main graph database class implementing `GraphSource<TSchema>` via WeakMap identity mapping (`#vertexIdentities`, `#edgeIdentities`) for object stability. Exports `Graph` with `addVertex`, `addEdge`, `deleteVertex`, `deleteEdge`, `updateProperty` methods; `Element` base class with `[$StoredElement]` symbol access; `Vertex` and `Edge` subclasses; `parsePropertyValue` for Standard Schema validation; `UniqueConstraintViolationError` on unique index conflicts.

**[GraphStorage.ts](./GraphStorage.ts)** — Storage abstraction layer. Exports `ElementId<TLabel>` template literal type `` `${TLabel}:${string}` ``, `GraphStorage` interface, `StoredElement`/`StoredVertex`/`StoredEdge` discriminated by `"@type"`, and `InMemoryGraphStorage` with bidirectional edge indexing (`#incomingEdges`, `#outgoingEdges` Maps) and cascade deletion.

**[AsyncGraph.ts](./AsyncGraph.ts)** — Async transport layer for remote graph execution. Exports `AsyncGraph<TSchema>` with `query()` method serializing steps via `jsonClone`, streaming through `config.transport`, and deserializing via `instantiateResult`. Discriminated union commands: `AsyncQuery`, `AsyncTransaction` with operations (`AddVertexOperation`, `AddEdgeOperation`, `DeleteVertexOperation`, `DeleteEdgeOperation`, `UpdatePropertyOperation`) tagged by `@type`.

## Query Execution Engine

**[Steps.ts](./Steps.ts)** — Step hierarchy and condition evaluation. Exports `Step<TConfig>` base class with `traverse()`, `clone()`, `toJSON()` methods; `ContainerStep` for nested steps; concrete step classes: `FetchVerticesStep`, `FetchEdgesStep`, `VertexStep`, `EdgeStep`, `FilterElementsStep`, `RangeStep`, `CountStep`, `SumStep`, `AvgStep`, `MinStep`, `MaxStep`, `CollectStep`, `GroupByStep`, `OrderStep`, `DedupStep`, `SelectStep`, `WithStep`, `UnwindStep`, `CallStep`, `RepeatStep`, `ShortestPathStep`, `ForeachStep`, `CreateStep`, `SetStep`, `DeleteStep`, `RemoveStep`, `MergeStep`, `UnionStep`, `IntersectStep`, `QueryUnionStep`, `MultiQueryStep`, `OptionalMatchStep`, `BindPathStep`, `CartesianFetchStep`, `MapElementsStep`, `ValuesStep`, `PropertyValuesStep`, `UnfoldStep`, `LabelsStep`, `ExpressionReturnStep`. Condition evaluation via `evaluateCondition()` supporting `["and" | "or" | "xor" | "not", ...]`, `["=", property, value]`, `["exists" | "isNull" | "isNotNull", property]`, `["in", property, values]`, `["=~" | "startsWith" | "endsWith" | "contains", property, string]`, and expression conditions.

**[Traversals.ts](./Traversals.ts)** — Gremlin-style fluent API with compile-time path type tracking. Exports `TraversalPath<TParent, TValue, TLabels>` immutable linked list with `with()`, `get()`, `getAll()`, `property()`, `nodes()`, `relationships()`, `length()`, `sum()` methods; `Traversal<TSchema, TPath>` base; `GraphTraversal` entry point with `V()`, `E()`, `union()`, `intersect()`; `VertexTraversal` with `in()`, `out()`, `both()`, `inE()`, `outE()`, `bothE()`, `has()`, `hasLabel()`, `as()`, `select()`, `repeat()`, `shortestPath()`; `EdgeTraversal` with `inV()`, `outV()`, `otherV()`, `bothV()`; specialized traversals `OrderVertexTraversal`, `RepeatVertexTraversal`, `ShortestPathTraversal`.

**[QueryContext.ts](./QueryContext.ts)** — Immutable query execution context. Exports `QueryContext<TSchema>` with `#params`, `#graph`, `#options` private fields; `getParam()`, `hasParam()`, `withParams()`, `withOptions()` methods; `QueryContextOptions` with `maxIterations?: number` (default `1000`) and `maxCollectionSize?: number` (default `100000`).

**[Comparator.ts](./Comparator.ts)** — Cross-type value ordering. Exports `compare()` with type precedence `undefined(0) < null(1) < boolean(2) < number(3) < string(4) < object(5)`; `compareObjects()` for recursive comparison; `isNumericConvertible()` for finite numbers, numeric strings, booleans.

## AST Transformation

**[astToSteps.ts](./astToSteps.ts)** — AST-to-Steps converter. Exports `astToSteps(query)`, `unionAstToSteps(unionQuery)`, `anyAstToSteps(ast)`, `multiStatementToSteps(multiStatement)`. Converts `Query` segments into step pipelines: `processQuerySegments()` for flexible clause ordering (MATCH...CREATE...WITH...MATCH), `processLegacyQuery()` for flat structures. Pattern conversion: `convertPattern()` for node/edge chains, `convertMultiPattern()` for Cartesian products, `convertShortestPathPattern()` for BFS/Dijkstra, `convertQuantifiedEdge()` for `*min..max` quantifiers. Mutation conversion: `convertCreateClause()`, `convertMergeClause()`, `convertSetClause()`, `convertDeleteClause()`, `convertRemoveClause()`.

## Registry Systems

**[FunctionRegistry.ts](./FunctionRegistry.ts)** — Built-in Cypher function registry. Exports `FunctionRegistry` class with case-insensitive lookup (`name.toLowerCase()`); `FunctionDefinition` with `category: "scalar"|"aggregate"|"list"|"type"|"math"|"string"|"temporal"`, `supportsDistinct`, `variadic`, and `impl`. Global instance `functionRegistry`, helpers `evaluateFunction()`, `isBuiltinFunction()`, `isAggregateFunction()`, `functionArgExpectsPath()`. 70+ built-in functions: `type()`, `labels()`, `id()`, `elementId()`, `properties()`, `startNode()`, `endNode()`, string functions (`toLower`, `substring`, `split`), math functions (`abs`, `ceil`, `round`, `rand`, `pi`), list functions (`head`, `tail`, `range`, `coalesce`), temporal functions (`date`, `datetime`, `duration`, `duration.between`), aggregate stubs (`count`, `sum`, `avg`, `min`, `max`, `collect`), path functions (`length`, `nodes`, `relationships`).

**[ProcedureRegistry.ts](./ProcedureRegistry.ts)** — Built-in Cypher procedure registry. Exports `ProcedureRegistry` with `register()`, `get()`, `invoke()`; `ProcedureDefinition` with `name` (supports namespaces like `db.labels`), `params`, `yields`, `impl` generator. Global `procedureRegistry`, helper `isBuiltinProcedure()`. Schema procedures: `db.labels`, `db.relationshipTypes`, `db.propertyKeys`, `db.schema.nodeTypeProperties`, `db.schema.relTypeProperties`. Utility procedures: `dbms.procedures`, `dbms.functions`.

## Supporting Infrastructure

**[Exceptions.ts](./Exceptions.ts)** — 14 graph-specific error classes extending `GraphError`. Lookup errors: `ElementNotFoundError`, `VertexNotFoundError`, `EdgeNotFoundError`, `LabelNotFoundError`. Type errors: `InvalidComparisonError`. Limit errors: `MaxIterationsExceededError(limit, step)`, `MemoryLimitExceededError(limit, actual)`. Schema errors: `PropertyValidationError`, `PropertyTypeError(key, label, value, issues)`, `UniqueConstraintViolationError(label, property, value, existingElementId)`, `AsyncValidationError`. Runtime: `ReadonlyGraphError(stepName)`, `GraphConsistencyError`.

**[generateSchemaGuide.ts](./generateSchemaGuide.ts)** — LLM prompt generators. Exports `generateGrammarDescription()` returning pure query language grammar (~350 lines); `generateSchemaGuide<TSchema>(schema)` injecting vertex/edge labels from runtime schema; `generateCompactSchemaGuide<TSchema>(schema)` for token-constrained contexts. Behavioral contracts: node patterns `(variable:Label)`, edge patterns `-[type]->`, variable-length `*1..3`, identifier format `<EntityName>:<uuid>`.

**[getDemoGraph.ts](./getDemoGraph.ts)** — Test fixture factory. Exports `DemoSchema` with vertices `Person` (`ref?: string`, `name`: string with `localeCompare`, `age`: number) and `Thing` (`ref`: number, `name`: string), edges `knows`/`likes`. `createDemoGraph()` returns `Graph<DemoSchema>` with 7 Person vertices, 7 Thing vertices, and connected edge network.

**[index.ts](./index.ts)** — **packages/graph/src/index.ts exposes graph query engine entry point with AST types, step execution, parsing, and readonly-safe query conversion.**

## Public Interface

AST types: Query, UnionQuery, MultiStatement, MatchClause, WhereClause, ReturnClause, ReturnItem, OrderByClause, OrderItem, Pattern, PatternElement, NodePattern, EdgePattern, Quantifier, PropertyCondition, ExistsCondition, AndCondition, OrCondition, ASTCondition, Literal, ShortestPathPattern, CreateClause, CreateNodePattern, ParameterRef, ArithmeticExpression, ArithmeticOperator, UnaryExpression, Expression, FunctionCall, UnwindClause, ASTUnwindExpression, SimpleCaseExpression, SearchedCaseExpression, CaseAlternative, SearchedCaseAlternative, ListLiteralExpr, ListIndexExpression, SliceExpression, ListComprehension, PatternComprehension, QuantifierExpression, ReduceExpression, DynamicPropertyAccess, MapProjection, MapProjectionSelector, MapPropertySelector, MapLiteralEntry, MapAllPropertiesSelector, MapVariableSelector, ExistsSubquery, LabelExpression, LabelName, LabelOr, LabelAnd, LabelNot, LabelWildcard, ParenthesizedPathPattern, SetClause, SetAssignment, SetAllProperties, SetAddProperties, SetValue, PropertyValue, PropertyMap, IsLabeledCondition, CallClause, YieldItem.

Core: `parseQueryToSteps(queryString, options?): { steps: readonly Step<any>[]; postprocess: (row: readonly unknown[]) => Record<string, unknown> }`. Parses via `parse`, converts via `anyAstToSteps`. Throws `ReadonlyGraphError` if `options.readonly` and mutation steps present.

Re-exports: `parse`, `astToSteps`, `anyAstToSteps`, `unionAstToSteps`, `multiStatementToSteps`, `Step`, `ContainerStep`, `ReadonlyGraphError`, `FunctionRegistry`, `functionRegistry`, `evaluateFunction`, `isBuiltinFunction`, `isAggregateFunction`, `functionArgExpectsPath`, `FunctionDefinition`, `FunctionCallContext`, `FunctionArgSpec`, `ProcedureRegistry`, `procedureRegistry`, `isBuiltinProcedure`, `ProcedureDefinition`, `ProcedureParamSpec`, `ProcedureYieldSpec`, `ProcedureCallContext`, plus all from `./Graph.js`, `./AsyncGraph.js`, `./GraphSchema.js`, `./GraphStorage.js`, `./Traversals.js`, `./Steps.js`, `./steps/index.js`, `./indexes/index.js`, `./generateSchemaGuide.js`.

## Behavioral Contracts

`MUTATION_STEP_NAMES`: Set with `"Create"`, `"Set"`, `"Delete"`, `"Remove"`, `"Merge"`, `"Foreach"`. `validateNoMutations(steps)` throws `ReadonlyGraphError(step.name)` if mutation step found, recurses `ContainerStep.steps`.

Postprocessing: groups properties by variable; aliases override variable names; expressions without aliases use `expr_${index}`; mixed property/value returns use `_value`; multi-statement queries pass through with `_statementIndex`; union queries use first query's RETURN clause; aggregate-only queries normalize non-array rows to `[row]`.

Options: `ParseQueryToStepsOptions` with `readonly?: boolean`.

## Integration Points

Barrel file for: `./AST.js`, `./astToSteps.js`, `./grammar.js`, `./Steps.js`, `./Exceptions.js`, `./Graph.js`, `./AsyncGraph.js`, `./GraphSchema.js`, `./GraphStorage.js`, `./Traversals.js`, `./indexes/index.js`, `./FunctionRegistry.js`, `./ProcedureRegistry.js`, `./generateSchemaGuide.js`.

## Annex Files (reproduction-critical constants)

- generateSchemaGuide.annex.sum

## Subdirectories

### [steps/](./steps/)

Modularized step implementations organized into 8 categories. Exports `StepRegistry`, `stepRegistry` singleton, `isKnownStep()`, `createStepFromJSON()`, and categorized step classes: `FetchVerticesStep`, `FetchEdgesStep`, `CartesianFetchStep` (fetch); `VertexStep`, `EdgeStep`, `RepeatStep`, `ShortestPathStep` (traversal); `FilterElementsStep`, `FilterPredicateStep`, `DedupStep` (filter); `MapElementsStep`, `ValuesStep`, `PropertyValuesStep`, `LabelsStep`, `SelectStep`, `UnfoldStep`, `BindPathStep`, `CallStep`, `ExpressionReturnStep` (transform); `CountStep`, `SumStep`, `AvgStep`, `MinStep`, `MaxStep`, `CollectStep`, `GroupByStep` (aggregate); `CreateStep`, `SetStep`, `DeleteStep`, `RemoveStep`, `MergeStep` (mutation); `RangeStep`, `OrderStep`, `OptionalMatchStep`, `WithStep`, `UnwindStep`, `ForeachStep` (control); `UnionStep`, `IntersectStep`, `QueryUnionStep`, `MultiQueryStep` (setops). JSON serialization format: `[stepName, config, nestedSteps?]`.

### [indexes/](./indexes/)

Index implementations for query optimization. Exports `HashIndex` (O(1) equality), `BTreeIndex` (O(log n) range), `FullTextIndex` (BM25 text search), `IndexManager` coordinating unique constraints and lazy index building, `QueryPlanner` for index hint selection, and type definitions.

### [test/](./test/)

Comprehensive test suite including unit tests for all modules and TCK (Technology Compatibility Kit) tests in `tck/` subdirectory covering Cypher specification compliance.

## Architecture

**Parser Pipeline:** `grammar.peggy` defines PEG rules → `grammar.js` (Peggy 5.1.0 generated) parses input → `AST.ts` discriminated unions represent syntax tree.

**Query Execution Pipeline:** `astToSteps.ts` transforms AST → `Step<any>[]` pipeline (fetch → filter → transform → aggregate) → `createTraverser(steps).traverse(graph, input, context)` executes via `Traverser` chaining `Step.traverse()` methods → `TraversalPath` immutable linked list tracks bindings.

**Data Flow:** `GraphTraversal` fluent API builds step chains → `parseQueryToSteps()` converts Cypher strings → `Graph` runtime executes against `GraphStorage` → `IndexManager` optimizes lookups via `HashIndex`/`BTreeIndex`/`FullTextIndex`.

**Async Layer:** `AsyncGraph` serializes steps to JSON → transport function → remote execution via `handleAsyncCommand` → deserialization via `instantiateResult`.

## Patterns

**Discriminated Unions:** AST nodes use `type` field tagging (`type: "Query"`, `type: "NodePattern"`). Transport types use `@type` (`@type: "AsyncQuery"`, `@type: "AddVertexOperation"`). Storage elements use `"@type": "Vertex"` vs `"Edge"`.

**Registry Pattern:** `FunctionRegistry` and `ProcedureRegistry` use `Map<string, Definition>` with lowercase keys for case-insensitive lookup. `StepRegistry` replicates pattern for step deserialization.

**Identity Mapping:** `Graph` uses `WeakMap<StoredVertex, Vertex>` and `WeakMap<StoredEdge, Edge>` to ensure object stability across traversals.

**Immutable Path Construction:** `TraversalPath.with(value, labels)` returns new instance rather than mutating, enabling branch-free history tracking.

**Step Pipeline:** All operations modeled as `Step<TConfig>` with `traverse(source, input, context): IterableIterator<unknown>`. `ContainerStep` holds nested `Step<any>[]` for compound operations (`RepeatStep`, `OptionalMatchStep`, `UnionStep`).

**JSON Serialization:** Steps serialize to `[name, config, steps?]` tuples. `createStepsFromJSON` deserializes via `KnownSteps` registry lookup.

## Behavioral Contracts

**ElementId Format:** Template literal `` `${TLabel}:${string}` ``. Parsing via `parseElementId()` splits on first `:`; `getLabelFromElementId()` iterates chars seeking colon. Error: `` `Invalid element id: ${id}` ``.

**Grammar Regex Patterns (grammar.js):**

- Identifier start: `/^[a-zA-Z_]/` (`peg$r0`)
- Identifier part: `/^[a-zA-Z0-9_]/` (`peg$r1`)
- Hex integer: `0x[0-9a-fA-F]+`
- Octal integer: `0o[0-7]+`
- Scientific: `[0-9]+(\.[0-9]+)?[eE][+-]?[0-9]+`
- String escapes: `\n`, `\r`, `\t`, `\\`, `\"`, `\'`

**Case-Insensitivity:** Keywords parsed via `"WORD"i !IdentifierPart` (negative lookahead `[a-zA-Z0-9_]`). Function/procedure names stored and looked up via `.toLowerCase()`.

**Temporal Parsing (TemporalTypes.ts + FunctionRegistry.ts):**

- Date regex: `/^(\d{4})-(\d{2})-(\d{2})$/` — error: `Invalid date string: ${val}. Expected format: YYYY-MM-DD`
- LocalTime regex: `/^(\d{2}):(\d{2}):(\d{2})(?:\.(\d{1,9}))?$/` — error: `Invalid localtime string: ${val}. Expected format: HH:MM:SS or HH:MM:SS.nnnnnnnnn`
- Time offset regex: `/^(\d{2}):(\d{2}):(\d{2})(?:\.(\d{1,9}))?(Z|([+-])(\d{2}):(\d{2}))$/` — error: `Invalid time string: ${val}. Expected format: HH:MM:SS+HH:MM, HH:MM:SS-HH:MM, or HH:MM:SSZ`
- Duration ISO 8601: `P[n]Y[n]M[n]DT[n]H[n]M[n]S` — error: `Invalid duration string: ${val}. Expected ISO 8601 format: P[n]Y[n]M[n]DT[n]H[n]M[n]S (e.g., P1Y2M3D, PT1H30M, P1DT12H)`

**Error Messages (Exceptions.ts):**

- `VertexNotFoundError`: `` `Vertex ${id} not found` ``
- `EdgeNotFoundError`: `` `Edge ${id} not found` ``
- `MaxIterationsExceededError`: `` `Maximum iterations (${limit}) exceeded in ${step}. Consider adding a LIMIT clause or increasing maxIterations.` ``
- `MemoryLimitExceededError`: `` `Collection size (${actual}) exceeds limit (${limit}). Consider adding a LIMIT clause or increasing maxCollectionSize.` ``
- `PropertyTypeError`: `` `Property '${key}' on label '${label}' failed validation: ${issueText}` `` (where `issueText = issues.join("; ")`)
- `UniqueConstraintViolationError`: `` `Unique constraint violation: property '${property}' on label '${label}' already has value ${JSON.stringify(value)} (existing element: ${existingElementId})` ``
- `ReadonlyGraphError`: `` `Query contains mutation step '${stepName}' but readonly mode is enabled` ``

**Safety Limits:** `DEFAULT_MAX_REPEATS = 1000` (RepeatStep), `DEFAULT_MAX_COLLECTION_SIZE = 100000` (CollectStep), `DEFAULT_MAX_GROUPS = 100000` (GroupByStep).

**Quantifier Emission:** Range quantifiers (`*1..3`) emit results at EACH hop within range; use `DISTINCT` to deduplicate.

**Aggregate Function Stubs:** `count`, `sum`, `avg`, `min`, `max`, `collect` in `FunctionRegistry` throw `Error("Aggregate functions must be handled at the step level")` when invoked in scalar context.

**Anonymous Variables:** Prefix `__anon_${counter}` (counter starts 0) for unnamed nodes in CREATE patterns.

**Step JSON Tuple Format:** `[stepName: string, config: object, nestedSteps?: Step[]]`. Container steps include third element; leaf steps omit. `FilterPredicateStep.fromJSON` and `MapElementsStep.fromJSON` always return `null` (non-serializable function predicates).

## Reproduction-Critical Constants

Full schema guide templates: [generateSchemaGuide.annex.sum](./generateSchemaGuide.annex.sum)
